import {readFile,writeFile,mkdir} from 'node:fs/promises';
import {createHash} from 'node:crypto';
import cases from '../../src/cases.js';
import {measurement,PROTOCOL_VERSION} from '../../src/measurement.js';
const dir=new URL('./',import.meta.url);
const esc=x=>String(x??'').replaceAll('&','&amp;').replaceAll('<','&lt;').replaceAll('>','&gt;').replaceAll('"','&quot;');
const table=(heads,rows)=>`<div class="scroll"><table><thead><tr>${heads.map(x=>`<th>${esc(x)}</th>`).join('')}</tr></thead><tbody>${rows.map(r=>`<tr>${r.map(x=>`<td>${x}</td>`).join('')}</tr>`).join('')}</tbody></table></div>`;
const code=x=>`<code>${esc(x)}</code>`;
const names={baseline:'基準',missing_info:'情報不足',off_focus:'焦点ずれ',overreach:'根拠を超えた一般化'};
const section=(id,title,body)=>`<section id="${id}" class="panel sub"><h2>${title}</h2>${body}</section>`;
const parts=[];
parts.push(section('question','何を調べる実験か',`<p>同僚から届いた報告案を確認し、自分の名前で上司へ提出する場面を設定する。文書内容の違いとAI使用の開示が、受け手の「今回の判断に十分な内容がある」という認知、送り手への信頼、確認・修正、作業負担にどう関わるかを調べる。Workslopの問題意識のうち、受け渡し後に受け手に残る仕事を扱う。</p><div class="story"><b>文書内容・AI開示の操作</b><span>↓</span><b>編集前の十分さの認知・送り手への信頼</b><span>↓</span><b>資料の確認・文書の修正・提出</b><span>↓</span><b>負担・提出文に残る不足・送り手への評価</b></div><p class="note">矢印は測定順序と検討する関係を表す。認知や信頼そのものは無作為化しないため、矢印のすべてを因果機序として確定できる設計ではない。送り手の作成時間、組織全体の生産性、自然なLLM出力における欠陥の発生率は測らない。</p>`));
parts.push(section('design','50人のパイロットと割付',`<div class="cards"><div class="card"><div class="metric">50人</div>募集上の完了目標</div><div class="card"><div class="metric">4案件／人</div>6テーマ中の異なる4テーマ</div><div class="card"><div class="metric">4条件</div>全員が内容条件を各1回</div><div class="card"><div class="metric">2群</div>AI開示あり／説明なし</div></div><p>4内容条件（参加者内）×2開示条件（参加者間）の混合計画。50人全員が完了すると200案件分になるが、独立した参加者は50人である。開示なしは「人間だけで作成」と伝える条件ではなく、作成方法の説明を表示しない条件である。</p>${table(['割付対象','現在のアルゴリズム','保証する範囲'],[['AI開示','参加開始時に各人独立に確率0.5で抽選。4案件を通じて固定。','25人ずつには固定しない。離脱に応じて確率を変えない。'],['内容の順序','Williamsの4系列のうち、同じ開示群で開始数の少ない系列を選び、同数なら無作為。','基準→情報不足→一般化→焦点ずれ／情報不足→焦点ずれ→基準→一般化／焦点ずれ→一般化→情報不足→基準／一般化→基準→焦点ずれ→情報不足。'],['テーマと位置','利用数の少ない4テーマを選び、テーマ×内容×開示の偏り（重み10）とテーマ×位置の偏り（重み1）が小さい並びを選ぶ。同点は無作為。','全員に同じテーマ・条件の組合せを固定しない。選定済み6テーマの範囲。'],['同時アクセス','割付時点で読み取った件数を参照する。','同時リクエスト間の厳密な均衡は保証しない。各人が4条件を1回ずつ経験することとは別。'],['再開・離脱','同じブラウザは同じ参加ID・割付で再開。開始済み参加は離脱しても割付集計に残る。','完了者の均衡は保証しない。別ブラウザでの重複参加は募集サイト側でも管理する。']])}<p>募集人数の制限はクラウドソーシング側で行い、アプリに人数による自動停止は置かない。公開入口は1つ。分析対象の期間は同意後の参加開始時刻 <code>sessions.created_at</code> で指定する。匿名のページ訪問時刻とは異なる。既存の検証参加も同じ内部集計に含まれるため、現在の割付履歴はゼロからではない。</p>`));
parts.push(section('stimuli','刺激：共通資料から作る4種類の報告案',`<p>各案件で依頼・用語説明・資料を共通にし、受け取る本文を変える。現在は6テーマ×1土台×4条件＝24本文。複数テーマはあるが、同一テーマ・同一条件内の独立した複数の生成文はまだない。AI開示の文言は「${esc(cases.common.disclosure.present)}」。初読・編集前認知・編集画面に表示する。</p>${table(['条件','研究上の操作意図','主に照合する知覚'],[['基準','依頼に応える方針、根拠、確認事項を備えた比較用本文。無欠陥だと自動認定するものではない。','十分さ6項目、六観点を比較の起点にする。'],['情報不足','判断根拠となる具体的情報を弱める／省く。','必要情報 perc_2。十分さへの波及も確認。'],['焦点ずれ','依頼された次の調査・試行方針への回答を、資料や制度の説明へ寄せる。','依頼への応答 perc_3。確認事項 perc_6 等への波及も確認。'],['一般化','元資料が支える範囲を超えて今回への効果・適用を述べる。','支持範囲の区別 perc_5、正確さ perc_1、確信表現 certainty。']])}<p>これらは操作意図であり、互いに完全に独立した認知次元だとは仮定しない。MCでは狙った差と併発する差の両方を見る。専門知識を要しにくい教示と資料を用意しているが、非専門家に理解可能かはパイロットで確認する。</p>${table(['テーマ','基準','情報不足','焦点ずれ','一般化'],cases.cases.map(c=>[esc(c.title),...cases.conditions.map(k=>c.bases[0].versions[k].paragraphs.join('\n\n').length)]))}<p class="note">表の文字数は表示本文のUTF-16長（段落間改行を含む）。長さは完全一致ではない。差が極端でないか、読みやすさ・自然さ・表面的完成度に意図しない差がないかを併せて評価する。文字数を後から共変量に入れるだけで操作の複合性が解消するとはみなさない。</p><details><summary>6テーマの依頼・資料・24本文と制作メタデータを見る</summary>${cases.cases.map(c=>`<article class="sub"><h3>${esc(c.title)}</h3><p>${esc(c.sourceDocument)}</p><p>${esc(c.request.lead)}</p>${c.request.paragraphs.map(p=>`<p>${esc(p)}</p>`).join('')}${c.sources.map(s=>`<details><summary>${esc(s.title)}</summary>${(s.excerpt||s.paragraphs).map(p=>`<p>${esc(p)}</p>`).join('')}</details>`).join('')}${c.bases.map(b=>`<p class="note">土台の制作記録：${esc(b.origin)}。これは保存済みメタデータの転記であり、独立に生成来歴を検証した表示ではない。</p>${cases.conditions.map(k=>`<details><summary>${names[k]} · ${b.versions[k].paragraphs.join('\n\n').length}文字</summary><p class="note">${esc(b.versions[k].status)}</p><div class="textdoc">${esc(b.versions[k].paragraphs.join('\n\n'))}</div></details>`).join('')}`).join('')}</article>`).join('')}</details><p class="note">制作記録ではAIによる盲検評価済み、人間による盲検採点は未完了となっている。実務での自然な発生頻度や、全モデルに共通する傾向の証拠とは区別する。</p>`));
parts.push(section('procedure','参加者が行う手順',`<p>架空の初対面の同僚から報告案を受け取り、画面内の資料だけで仕上げる。外部検索・生成AIの利用は禁じる。送り手名は提示順に佐藤・鈴木・高橋・田中。名前自体の独立効果は推定しない。</p>${table(['段階','参加者の作業','記録・注意'],[['開始','同意、年齢・性別、役割説明、業務経験・AI利用・一般的信頼傾向。','参加開始時刻、同意スナップショット、背景回答。'],['① 依頼・資料','今回求めるのは本格導入の決定ではなく、次の調査・試行の方針だと確認。2問の理解確認。','全問正答まで繰り返し。各試行回答を保存。'],['② 初読','報告案を読む。資料を開けるが、まだ編集しない。','表示・資料開閉・スクロール等。'],['③ 編集前評価','テーマの読了確認1問＋十分さ6、表面的完成度2、送り手への信頼4。','本文と資料を参照可能。この評価自体が精査を促す可能性がある。'],['④ 編集・提出','必要なら修正し、自分の名前で提出。無修正も可。','入力差分、自動保存、初期文・提出文。'],['⑤ 提出後評価','精神的努力1＋RTLX6、事後信頼4、文書の知覚9、責任帰属等9。','文書知覚では初期本文を再表示。責任帰属は作業後の回顧。'],['繰返し','①〜⑤を異なる4テーマで実施。','前の案件へ戻って提出内容を変更しない。'],['終了','全体の自由記述1、開示の想起・AI認識等6、終了キーワード。','想起は4案件全体について1回。任意記述2項目を含む。']])}<p>教示の理解確認が一般化の境界を教えるため、この実験の推定対象は<strong>目的と資料の限界を明示された受け手</strong>である。無教示の職場への効果量の外挿は別途必要。旧版にあった根拠説明・条件適用の独立課題（U1/U2）は現行フローでは実施しない。</p><p>背景9、各案件44（理解確認2・テーマ確認1を含む）×4、最後7＝通常192回答項目。うち2つは任意記述。年齢・性別・同意は別。理解確認の再試行で増える。人間の所要時間は未測定であり、パイロットで所要時間・離脱・後半の回答の単調化を必ず点検する。</p>`));
parts.push(section('outcomes','指標と集計方法',table(['位置づけ','指標・時点','保存する項目','集計・解釈'],[['主要','編集前の判断に必要な内容の十分さ','suff_1〜suff_6 / cognition_i','suff_2を8−xで逆転し6項目の平均（1–7）。全項目が有効値の場合。AIMQ完全性の研究用翻訳・対象変更。'],['副次','表面的な完成度','comp_1, comp_2 / cognition_i','まず各項目を別々に表示。十分さとのずれを見る。自動的にworkslop得点へ合算しない。'],['副次','送り手への信頼・前後差','tr_1〜tr_4 / cognition_i, trust_post_i','tr_1とtr_3を8−xで逆転し平均。前は初読後＝処置後であり、処置前信頼ではない。前後差は信頼の更新を表す。'],['副次','精神的努力・負荷','effort_total、tlx_md/pd/td/op/ef/fr / post_i','努力0–10を独立報告。RTLXは6項目の無加重平均0–100、下位尺度も報告。成績は高いほど悪い向きで収集し追加逆転しない。'],['MC・探索','文書の六観点と自然さ等','perc_1〜6, read_ease, natural, certainty / perception_i','各1–7の単項目を別々に扱う。狙った知覚差と副作用を確認。初期文を示すが編集後の判断である。'],['探索','責任帰属・補完する動機','responsibilityの9項目、resp_free','最初の8項目は1–7、influenceは0–6（3＝影響なし）。合成尺度化せず別々に扱う。機序は本人の回顧的説明であり因果の証明ではない。'],['MC・探索','開示の記憶・AI使用の推測','memory, disc_confidence, belief, disc_attention, disc_influence, disc_free','memoryは0始まりの選択番号。他は質問ごとの0–10／0–6。開示なしでもAIだと思った可能性を分ける。'],['処置前記述','経験・一般的信頼傾向','bg_*、ptt_1〜4 / intro','信頼傾向は4項目平均。年齢・性別を含め標本を記述。50人で多数の調整変数を一度に入れない。']])+`<p>既存尺度名の使用は、この日本語・同僚・報告案という文脈で妥当性が確立したことを意味しない。コード上も研究用翻訳で、逆翻訳未実施とされる。パイロットでは項目分布・天井床効果・欠測・内的一貫性を確認し、多水準の構造妥当性まで50人で確定しない。</p>`));
parts.push(section('behavior','行動と成果物：保存と採点を分ける',table(['得たい指標','今保存されるもの','分析時に必要な処理'],[['資料の確認','source_open/close、scroll、画面・時刻。','開閉回数や表示区間を導出。開いただけで内容を検証・理解したとはみなさない。'],['編集量・推移','editor_inputの差分と選択位置、editor_snapshot、初期文・提出文。','挿入／削除の累積、最終差分、意味上の修正を分ける。入力回数を修復の質とみなさない。'],['作業時間','phase_render、action_requested、client_mono、page_id、visibility、focus等。','初読と編集の区間を別々に算出し質問回答時間を除く。非表示・通信待ち・再読込を区別。正確な思考時間は測れない。'],['提出時の残存不足','session_cases.initial_text / submitted_text。','依頼・元資料に対応した基準で初期文と提出文を盲検採点。現行アプリは自動で品質得点を保存しない。'],['意思決定への影響','報告書に記された次の調査・試行方針とその理由。','最終文から方針・理由・留保・確認事項の十分さを評価できる。独立した選択課題や実組織の意思決定成果は未測定。']])+`<p>採点の候補は①依頼への方針回答、②根拠と方針の関係、③支持範囲・留保、④次の確認事項。案件別の例と許容回答を固め、条件・開示・主観評定を伏せて初期／提出を同じ基準で評価する。複数評定者の一致度と不一致処理を定める。<strong>これは必要な分析手続の提案であり、今回の通しテストで採点済みという意味ではない。</strong></p><p>作業が少ないことを良い成果と同一視しない。「負担をかけて不足を解消」「少ない作業で不足が残存」などを分ける。変化量だけでは初期品質の違いが影響するため、初期・最終水準も併記する。</p>`));
parts.push(section('analysis','パイロットでの分析と判断',`<p>主要得点について、内容条件、開示、交互作用、テーマ、提示位置を説明変数にし、参加者内の反復を考慮する。参加者ランダム切片を含む混合モデルを出発点とし、小標本での推定の安定性を点検する。200案件を独立な200人と扱わない。</p><p>基準に対する3つの内容比較を事前指定する。検定を行う場合は目的ごとの比較群を定め多重性を調整する。50人はMC・所要時間・離脱と差の推定精度を調べるパイロットとして扱い、とくに開示との交互作用や媒介分析の確証には使わない。非有意を「差なし」とせず推定値と信頼区間を示す。</p><p>MCでの狙った差・併発差、認知項目の天井床効果、自然さ、読みやすさ、提示位置ごとの疲労と離脱を確認する。MC不通過者を事後的に除いて望ましい効果を作らない。具体的な採否閾値・除外基準・最小重要差は未確定であり、人間の結果を見る前に記録する必要がある。</p><p>開始・完了・中止・未完了を開示群／離脱画面別に報告する。途中データの扱いと感度分析を先に決める。混合モデルだけで非ランダムな離脱が解消するとはしない。6テーマを越えた業務全体への一般化はこの設計だけでは確定しない。</p><p>認知と行動、責任帰属と行動の関連は探索できるが、処置後認知を主要な処置効果の調整変数に機械的に入れない。信頼の低下が補完を増やす場合と減らす場合を、実測前に一方向へ決めつけない。</p>`));
parts.push(section('data','記録の粒度と確認できた範囲',`<p>Cloudflare Workers + D1に保存。<code>sessions</code>を親とし、<code>session_cases</code>、<code>responses</code>、<code>actions</code>、<code>events</code>を参加IDで結ぶ。<code>versions</code>に版・刺激・質問を保存する。質問・刺激は収集中に変更せず、採用版を固定する。</p><p>各入力イベントの追加・削除差分（UTF-16）と操作時刻を保存する。スクロールは500ms間隔に抑制、heartbeatは60秒間隔。現行実装は全keydown・全pointermove・IMEの個別compositionイベントを収集していない。入力イベントの<code>isComposing</code>は保存される。「すべての物理操作を最小粒度で取得」とは表現しない。</p><p>イベントIDとページ内連番で再送重複を除き、端末の未送信キューから送る。サーバー受信時刻と操作発生時刻は異なる。強制終了・端末データ消去まで無損失を保証するものではない。</p><p><a href="rawdata-viewer.html">実データ形式のHTML</a>と<a href="rawdata.json">JSON</a>を併せて確認できる。今回の合成テストでは50人同時の開始・5000イベント保存と再送、1人の4案件通し（回答35行、遷移35行、イベント648行）、途中再読み込みと提出文一致を確認した。50人が長時間ブラウザで作業し続けた試験ではなく、人間の負担・刺激妥当性・検出力を確認した結果でもない。</p><p>2026-09-21 15:42:51–15:42:55 JSTの負荷テスト50件と15:45:49–15:46:14 JSTの通しテスト1件は検証用。厳密なID一覧は remote-load-results.json と e2e-results.json に保存し、研究分析から除外する。本文は架空のテスト入力。</p>`));
parts.push(section('items','質問項目の全文と実装上の出典',`<p>以下は現行 <code>measurement.js</code> から自動転記。{sender}等は画面で置換される。出典はコードに記載された書誌メタデータで、このHTML作成で原尺度の全文照合を新たに完了したという意味ではない。</p>${Object.entries(measurement.blocks).map(([key,b])=>`<details><summary>${esc(b.title)} · ${key} · ${b.questions.length}項目</summary><p>${esc(b.instruction)}</p>${table(['ID','質問文','範囲・選択肢','出典・逆転'],b.questions.map(q=>[code(q.id),esc(q.text),esc(q.options?.map((v,i)=>`${i}: ${v}`).join(' / ')||q.ends||'任意記述'),esc(q.source)+(q.reverse?' / 逆転':'')]))}</details>`).join('')}<details><summary>尺度の書誌メタデータ</summary>${table(['略号','実装記載'],Object.entries(measurement.sources).map(([k,v])=>[code(k),esc(v)]))}</details>`));
parts.push(section('status','版・未確定事項',`<p>作成日：2026-09-21。対象プロトコル：${code(PROTOCOL_VERSION)}。本書は現行実装の説明と分析方針案を区別した研究者向け資料であり、参加者画面には公開しない。</p><ul><li>現在の教示・192項目で人間が無理なく完了できるか：今回の50人で確認する。</li><li>採点基準、MCの採否閾値、除外・欠測ルール：人間の結果を見る前に確定する。</li><li>研究用翻訳の理解しやすさ、刺激の人間による盲検点検：未完了。</li><li>実施者・問い合わせ先・保存期間等：募集案内・同意表示の具体的記載を揃える。</li></ul><p>正本：src/worker.js、src/measurement.js、src/cases.js、public/app.js。旧PROTOCOL_AND_LOGS.mdのv4記述や未使用understandingフィールドは現行測定に数えない。</p>`));
const checkAppendix=`<details><summary>理解確認・読了確認・年齢と性別・回答UI</summary>${table(['項目','質問・選択肢','回答規則'],cases.common.precheck.map((q,i)=>['理解確認'+(i+1),esc(q.question)+'<br>'+q.options.map((s,j)=>esc(j+': '+s)).join('<br>'),'正答番号 '+q.answer+'。全問正答まで再試行。誤答時：「答えが説明と合っていません。説明を読み直して、もう一度答えてください。」']))}<p>読了確認：「いま読んだ報告案は、何についての報告案でしたか。」現在のテーマを含む4テーマのタイトルから選ぶ。正答フラグを保存し、誤答でも次へ進む。</p><p>年齢は18–99歳の整数で必須。性別は0＝男性、1＝女性、2＝その他、3＝回答しないから選択必須。sessions.meta_json に age / gender として保存する。</p><p>RTLXは0–100、5刻み。初期つまみ位置は50だが表示は「未回答」で、操作するまで回答として受理しない。他の選択項目は初期選択なし。同意評定に「判断できない」はない。</p><p>同意画面の回答教示：「本調査には正解・不正解はありません。報告案や資料を読んで感じたこと、判断したことに基づいて、できるだけ普段どおりに回答してください。回答や提出内容の良し悪しで報酬が減ることはありません。」表示所要時間は30〜60分（人間の実測値ではなく案内値）。</p></details>`;
parts[8]=parts[8].replace('</section>',checkAppendix+'</section>');
const css=await readFile(new URL('methods-style.css',dir),'utf8');
const html=`<!doctype html><html lang="ja"><meta charset="utf-8"><meta name="viewport" content="width=device-width, initial-scale=1"><title>Workslop実験｜方法・指標</title><style>${css}html{scroll-behavior:smooth}summary{padding:12px;background:#f5f8fc;margin:7px 0;border-radius:7px}section{scroll-margin-top:25px}.story{display:flex;align-items:center;flex-direction:column;padding:20px;background:#eef3fc;border-radius:10px}.story span{color:#557ab1}nav{position:static}nav a{color:#245ac2;padding:7px;text-decoration:none}li{margin:8px 0}@media print{details{display:block}nav{display:none}}</style><header><strong>WORKSLOP / METHODS</strong><span>研究者用・現行実装の説明</span></header><main><div class="eyebrow">PILOT & MEASUREMENT · 2026.09.21</div><h1>何を、いつ、どう測るか</h1><p class="lede">4つの文書条件とAI開示を用いた、文書引継ぎ実験の方法・測定指標・分析上の境界。</p><div class="notice">実装済みの収集方法と、今後行う採点・分析を区別して記載しています。動作テストの合成データを人間の研究結果として扱いません。</div><nav>${[['question','問い'],['design','割付'],['stimuli','刺激'],['procedure','手順'],['outcomes','尺度'],['behavior','行動・品質'],['analysis','分析'],['data','保存'],['items','全質問'],['status','未確定']].map(([i,n])=>`<a href="#${i}">${n}</a>`).join('')}<a href="rawdata-viewer.html">実際のrawdata ↗</a></nav>${parts.join('\n')}<footer class="footer">ローカル閲覧資料。公開参加ページへの変更は行っていません。</footer></main></html>`;
await writeFile(new URL('methods-measures.html',dir),html);
await mkdir(new URL('independent-review-input/',dir),{recursive:true});
await writeFile(new URL('independent-review-input/methods-measures.html',dir),html,{flag:'wx'}).catch(e=>{if(e.code!=='EEXIST')throw e});
const paths=['../../src/worker.js','../../src/measurement.js','../../src/cases.js','../../public/app.js','methods-measures.html'];
const hashes=[];for(const p of paths)hashes.push({file:p,sha256:createHash('sha256').update(await readFile(new URL(p,dir))).digest('hex')});
await writeFile(new URL('methods-source-manifest.json',dir),JSON.stringify({createdAt:new Date().toISOString(),protocol:PROTOCOL_VERSION,files:hashes,reviewInput:['independent-review-input/methods-measures.html'],notProvided:['conversation','prior reviews','author decisions','implementation source code']},null,2));
console.log('Created methods-measures.html and independent review snapshot');
